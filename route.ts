import type{EmailOtpType}from"@supabase/supabase-js";import{NextResponse,type NextRequest}from"next/server";import{createClient}from"@/lib/supabase/server";
export async function GET(request:NextRequest){const token_hash=request.nextUrl.searchParams.get("token_hash");const type=request.nextUrl.searchParams.get("type")as EmailOtpType|null;const wanted=request.nextUrl.searchParams.get("next")||"/gestion";const next=wanted.startsWith("/")&&!wanted.startsWith("//")?wanted:"/gestion";if(token_hash&&type){const s=await createClient();const{error}=await s.auth.verifyOtp({type,token_hash});if(!error)return NextResponse.redirect(new URL(next,request.url));}return NextResponse.redirect(new URL("/acceso?error=enlace",request.url));}
