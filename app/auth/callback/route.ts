import{NextResponse,type NextRequest}from"next/server";import{createClient}from"@/lib/supabase/server";
export async function GET(request:NextRequest){const code=request.nextUrl.searchParams.get("code");const wanted=request.nextUrl.searchParams.get("next")||"/gestion";const next=wanted.startsWith("/")&&!wanted.startsWith("//")?wanted:"/gestion";if(code){const s=await createClient();const{error}=await s.auth.exchangeCodeForSession(code);if(!error)return NextResponse.redirect(new URL(next,request.url));}return NextResponse.redirect(new URL("/acceso?error=enlace",request.url));}
