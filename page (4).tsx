import Image from"next/image";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{UpdatePasswordForm}from"./form";
export default async function UpdatePasswordPage(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/recuperar-contrasena");return <main className="form-page"><section className="form-shell"><div className="form-brand"><Image src="/logo-aoma.png" width={45} height={45} alt="AOMA"/><div><strong>SIGCA</strong><span>AOMA SECCIONAL SAN JUAN</span></div></div><p className="kicker">NUEVA CONTRASEÑA</p><h1>Protegé tu cuenta</h1><p className="form-intro">Elegí una contraseña de al menos ocho caracteres.</p><UpdatePasswordForm/></section></main>}
